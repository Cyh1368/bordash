# Bordash

A lightweight one-page task dashboard with local JSON file persistence.

## Run

```bash
python3 server.py
```

Then open `http://127.0.0.1:8000`.

Tasks are stored in `tasks.json`. Projects are stored in `projects.json`.
